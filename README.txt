code-extract.py is the main script for parsing files. It has its own hopefully
somewhat self-explanatory help information 
  "python code-extract.py -h"

I've already run the analysis on some of the .CSV's with output in HTMLoutput.
Just open HTMLoutput/index.html in a browser. 